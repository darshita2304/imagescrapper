conda create -n image_Scrap 
python==3.6
-> it will create virtual environment for this project


conda activate image_Scrap
-> activate environmat

pip install -r reuirements.txt
-- to install all reuired lib

git init
git add .
git commit -m 'first commit'
git branch -M main

git config --global user.name "darshita2304"
git config --global user.email "emailaddress"

git remote add origin https://....git path
git push -u origin machine

# to clone somethin
git clone http://......git path